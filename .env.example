# Copy to `.env` and fill in real values. `.env` is gitignored.
# Docker Compose auto-loads `.env` from this directory.

# Telegram bot token for the Claude Code Telegram channel plugin.
# Obtain from @BotFather on Telegram.
TELEGRAM_BOT_TOKEN=
